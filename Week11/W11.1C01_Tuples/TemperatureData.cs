public class TemperatureData
{
    public (double, double) GetSalaryRange(double v1, double v2)
    {
        double minSalary = v1;
        double maxSalary = v2;
        return (minSalary, maxSalary);
    }
}